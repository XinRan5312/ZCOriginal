<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="5dip" />

            <gradient
                android:angle="0"
                android:centerColor="@color/progressbar_init"
                android:centerY="0.75"
                android:endColor="@color/progressbar_init"
                android:startColor="@color/progressbar_init" />
        </shape>
    </item>
    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="5dip" />

                <gradient
                    android:angle="0"
                    android:centerColor="@color/progressbar_init"
                    android:centerY="0.75"
                    android:endColor="@color/progressbar_init"
                    android:startColor="@color/progressbar_init" />
            </shape>
        </clip>
    </item>
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="5dip" />

                <gradient
                    android:angle="0"
                    android:endColor="@color/progressbar_had"
                    android:startColor="@color/progressbar_had" />
            </shape>
        </clip>
    </item>

</layer-list>